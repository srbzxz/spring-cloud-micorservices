spring:
  cloud:
    config:
      server:
        native:
          search-location: classpath:/services
  profiles:
    active: native
  security:
    user:
      password: admin

server:
  port: 8081